# Talos-executable

```
tar -xzf talos-linux-x64.tar.gz
cd talos-package
chmod +x talos

# Instalar globalmente (recomendado)
sudo cp talos /usr/local/bin/

# Ahora funciona desde cualquier carpeta:
talos --help
talos hello --name "Instalado"```